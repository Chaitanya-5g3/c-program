#include <stdio.h>
int main()
{
  int I, j;
  for(I=1;I<=5;I++)
  {
    for(j=1;j<=I;j++)
    {
      printf("* \t");
    }
    printf("\n");
  }
  for(I=5;I>=1;I--)
  {
    for(j=1;j<=I;j++)
    {
      printf("* \t");
    }
    printf("\n");
  }
  return 0;
}