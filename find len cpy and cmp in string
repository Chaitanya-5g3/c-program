#include<stdio.h>
void main()
{
  char str1[50], str2[50], str3[50];
  int i, j;
  scanf("%s",str1);
  scanf("%s",str2);
  scanf("%s",str3);
  int l1=0, l2=0;
  for(i=0;str1[i]!='\0';i++)
  {
    l1++;
  }
  printf("length of str %d\n",l1);
  for(i=0;str2[i]!='\0';i++)
  {
    str1[i]=str2[i];
  printf(" %c",str1[i]);
  
  }
  printf("\n");
  for(i=0;str3[i]!='\0';i++)
  {
    l2++;
  }
  
  if(l1==l2)
  {
    for(i=0;i<l1;i++)
    {
      if(str1[i]==str3[i])
      {
        printf("equal");
      }
      else
      {
        printf("not equal");
      }
    }
  }
  else
  {
   printf("not equal");
  }
  }