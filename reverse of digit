#include <stdio.h>
#include<stdlib.h>

int main()
{
  int num, I, sum=0, r, rev=0;
  printf("entet a digit");
  scanf("%d",&num);
  while(num>0)
    {
      r=num%10;
      rev=rev*10+r;
      num=num/10;
      
    }
    printf("the reverse of digit is %d",rev);
  return 0;
}