#include <stdio.h>
void main()
{
  char str[50];
  int i, l=0;
 gets(str);
  for(i=0;str[i]!='\0';i++)
  {
    if(str[i]!=' ')
    {
      printf("'%c' \t",str[i]);
      l++;
    }
    else
    {
      
    }
  }
  printf("\n");
  printf("length of the statement %d",l);
  
}