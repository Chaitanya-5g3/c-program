#include <stdio.h>
int main()
{
  int I, j, k;
  for(I=1;I<=5;I++)
  {
    for(k=I;k<=5;k++)
    {
      printf(" ");
    }
      for(j=1;j<=I;j++)
      {
        printf("*\t");
      }
      printf("\n");
  }
  return 0;
}