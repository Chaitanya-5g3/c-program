#include <stdio.h>

int main()
{
  int n, I, sum=0;
  printf("enter a number");
  scanf("%d",&n);
  for(I=0;I<n;I++)
  {
    if(I%2==0)
    {
      sum=sum+(I*I*I);
    }
  }
  printf("%d",sum);
  return 0;
}